import styled from "styled-components";

export const Dropdownitem = styled.li`
  display: flex;
  margin: 1rem auto;

  img {
    max-width: 2rem;
    margin-right: 1rem;
    opacity: 0.5;
    transition: ease-in-out;
  }

  a {
    max-width: 10rem;
    margin-left: 1rem;
  }
`;
